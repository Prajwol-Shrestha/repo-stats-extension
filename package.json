{
  "name": "github-stats-extension",
  "private": true,
  "scripts": {
    "build": "bun build src/*.ts --outdir dist --target browser",
    "dev": "bun build src/*.ts --outdir dist --target browser --watch"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/chrome": "^0.1.33"
  },
  "peerDependencies": {
    "typescript": "^5"
  }
}
